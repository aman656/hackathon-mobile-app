import { View, StyleSheet } from "react-native";
const UserRequestScreen = (props) => {
  return <View style={styles.container}></View>;
};
const styles = StyleSheet.create({
  container: {},
});
export default UserRequestScreen;
